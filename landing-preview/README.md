# CloudBoard landing HTML draft

Open `dist/index.html` directly, or serve `dist` with a local HTTP server.
The HTML, stylesheet, JavaScript, fonts and images are local files; no package installation is required.
The ZIP places index.html and its supporting assets at the root.

## Included
- Responsive Korean landing page, using the app's white and lavender palette.
- App screenshot tabs, keyboard navigation, enlarged screenshot dialog and FAQ.
- Monthly list price KRW 99,000 / pilot KRW 49,000, VAT-inclusive draft copy.
- Pilot duration is not finalized; contact copy does not promise a fixed term.
- Contact form composes a mailto message; it does not submit to a server or send email automatically.
- No subscription checkout, analytics, customer-data storage or production deployment.

## Assets / before publication
- `assets/studio-hero.png` is AI-generated illustrative studio imagery, visibly labeled on the page. It is not a real customer installation or a recording of the product.
- Product screenshots are existing local app QA captures with example content; replace them with final marketing captures before publication.
- A real demo video has not been recorded. The page honestly labels the product preview and contains no fake video player.
- Fonts: bundled Pretendard; see `assets/FONT-LICENSE.txt`.
- Confirm public contact address, pilot terms, price inclusions and hardware support before public release.
- Existing privacy/account-deletion URLs and supplied Google Play install URL are preserved.
